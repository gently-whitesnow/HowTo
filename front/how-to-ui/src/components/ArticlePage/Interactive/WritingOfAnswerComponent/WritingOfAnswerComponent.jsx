import { observer } from "mobx-react-lite";
import { WritingOfAnswerComponentWrapper } from "./WritingOfAnswerComponent.styles";

const WritingOfAnswerComponent = (props) => {
  return (
    <WritingOfAnswerComponentWrapper>WritingOfAnswerComponent</WritingOfAnswerComponentWrapper>
  );
};

export default observer(WritingOfAnswerComponent);
